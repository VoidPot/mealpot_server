type Mutation {
  login(
    username: String!
    password: String!
    roles: [CONNECTION_ROLE]!
  ): Response!
  storeLogin(storeId: Int!): Response!
}

enum CONNECTION_ROLE {
  ADMIN
  MANAGER
  BILLER
  KITCHEN
  OTHER
}
