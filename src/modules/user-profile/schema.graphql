type UserProfile {
  id: String!
  user: User!
  userId: String!
  profile: Profile!
  profileId: String!
  role: USER_PROFILE_ROLE!

  deleted: Boolean
  deletedAt: DateTime
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum USER_PROFILE_ROLE {
  ADMIN
  MANAGER
  AUTHOR
}
